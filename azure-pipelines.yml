---
trigger:
  - none

jobs:
  - job:
    steps:
    - script: |
        cat $(Build.Repository.LocalPath)/policy.txt
        echo "___________________________"
        cat $(Build.Repository.LocalPath)/policy.xml