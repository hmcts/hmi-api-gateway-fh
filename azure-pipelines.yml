parameters:
- name: environment
  type: string
  default: 'sbox,test,stg'

trigger: none

jobs:
- ${{ if contains(parameters.environment, 'sbox') }}:
  - job: sbox
    steps:
    - script: echo Building sbox...
- ${{ if contains(parameters.environment, 'test') }}:
  - job: test
    steps:
    - script: echo Building test...
- ${{ if contains(parameters.environment, 'stg') }}:
  - job: stg
    steps:
    - script: echo Building stg...
