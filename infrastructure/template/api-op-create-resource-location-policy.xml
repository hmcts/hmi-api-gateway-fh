<policies>
    <inbound>
        <base />
        <choose>
            <when condition="@(context.Request.Headers.GetValueOrDefault("Destination-System","") == "CFT")">
                <rewrite-uri template="/create-resource" copy-unmatched-params="true" />
                <set-backend-service base-url="http://hmi-casehqemulator.sandbox.platform.hmcts.net/resources/" />
            </when>
            <when condition="@(context.Request.Headers.GetValueOrDefault("Destination-System","") == "S&L")">
                <rewrite-uri template="/create-resource" copy-unmatched-params="true" />
                <set-backend-service base-url="http://#{sAndLHost}#/resources/" />
            </when>
            <when condition="@(context.Request.Headers.GetValueOrDefault("Destination-System","") == "CRIME")">
                <rewrite-uri template="/create-resource" copy-unmatched-params="true" />
                <set-backend-service base-url="http://hmi-casehqemulator.sandbox.platform.hmcts.net/resources/" />
            </when>
            <when condition="@(context.Request.Headers.GetValueOrDefault("Destination-System","") == "MOCK")">
                <set-backend-service base-url="https://www.hmcts.com/future-hearings/resources/update-resources-location" />
                <choose>
                    <when condition="@(context.Request.Headers.GetValueOrDefault("_EnableMocks", "false")=="true")">
                        <mock-response status-code="201" content-type="application/json" />
                    </when>
                </choose>
            </when>
        </choose>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>