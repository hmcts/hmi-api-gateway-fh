<policies>
    <inbound>
        #{ipWhitelist}#
        <return-response>
            <set-status code="200" />
            <set-header name="Content-Type" exists-action="override">
                <value>application/json</value>
            </set-header>
            <set-body>@( 
                new JObject(new JProperty("statusCode", 200),
                new JProperty("fromFD", context.Request.Headers.GetValueOrDefault("X-Azure-ClientIP","")),
                new JProperty("yourIP", context.Request.Headers.GetValueOrDefault("X-Forwarded-For",""))).ToString())</set-body>
        </return-response>
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>