<policies>
    <inbound>
        <set-variable name="req" value="@(context.Request.Body.As<JObject>())" />
        <choose>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Case Title") || ((JObject)context.Variables["req"]).GetValue("Case Title").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Case Title'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Case Id") || ((JObject)context.Variables["req"]).GetValue("Case Id").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Case Id'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("TransactionIDHMCTS") || ((JObject)context.Variables["req"]).GetValue("Case Id").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'TransactionIDHMCTS'")
                        ).ToString();
                        }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Venue") || ((JObject)context.Variables["req"]).GetValue("Venue").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Venue'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Jurisdiction") || ((JObject)context.Variables["req"]).GetValue("Jurisdiction").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Jurisdiction'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Service") || ((JObject)context.Variables["req"]).GetValue("Service").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Service'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Case Type") || ((JObject)context.Variables["req"]).GetValue("Case Type").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Case Type'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Hearing Type") || ((JObject)context.Variables["req"]).GetValue("Hearing Type").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Hearing Type'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Hearing channel") || ((JObject)context.Variables["req"]).GetValue("Hearing channel").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Hearing channel'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Private Hearing") || ((JObject)context.Variables["req"]).GetValue("Private Hearing").Type != JTokenType.Boolean)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Private Hearing'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
            <when condition="@(!((JObject)context.Variables["req"]).ContainsKey("Allocated Listing Team") || ((JObject)context.Variables["req"]).GetValue("Allocated Listing Team").Type != JTokenType.String)">
                <return-response>
                    <set-status code="400" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                        new JProperty("Error", "400"),
                        new JProperty("Reason", "Malformed request. Missing/Invalid property: 'Allocated Listing Team'")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
        </choose>
        <base />
        <set-backend-service base-url="https://www.hmcts.com/request-hearings/request-listings" />
        <choose>
            <when condition="@(context.Request.Headers.ContainsKey("_EnableMocks"))">
                <mock-response status-code="200" content-type="application/json" />
            </when>
        </choose>

    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>