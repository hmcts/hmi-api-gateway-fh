<policies>
    <inbound>
        <base />
        <choose>
            <when condition="@(context.Request.Method == "GET")">
                <return-response>
                    <set-status code="200" reason="Malformed request." />
                    <set-body>@{
                        return new JObject(
                            new JProperty("status code", "200"),
                            new JProperty("description", "The platform has recognised the request type.")
                        ).ToString();
                    }</set-body>
                </return-response>
            </when>
        </choose>
        <set-backend-service base-url="https://www.hmcts.com/request-hearings/sessions" />
        <mock-response status-code="200" content-type="application/json" />
    </inbound>
    <backend>
        <base />
    </backend>
    <outbound>
        <base />
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>