parameters:
  - name: zipName
    type: string

  - name: directory
    type: string

steps:
  - script: |
      zip $(System.DefaultWorkingDirectory)/$(Build.BuildId)/$(Build.BuildId)-${{ parameters.zipName }}.zip $(System.DefaultWorkingDirectory)${{ parameters.directory }}
    condition: succeededOrFailed()
